# requested through the portal, don't hand-edit - raise a new request instead
apiVersion: platform.example.org/v1alpha1
kind: ResourceGroup
metadata:
  name: rg-deploy-testing
spec:
  resourceGroupName: rg-deploy-testing
  location: westeurope
  environment: dev
  team: team-alpha
