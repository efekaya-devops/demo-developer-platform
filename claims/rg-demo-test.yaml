# requested through the portal, don't hand-edit - raise a new request instead
apiVersion: platform.example.org/v1alpha1
kind: ResourceGroup
metadata:
  name: rg-demo-test
spec:
  resourceGroupName: rg-demo-test
  location: westeurope
  environment: dev
  team: team-alpha
